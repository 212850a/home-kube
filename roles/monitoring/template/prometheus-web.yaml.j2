apiVersion: v1
kind: Service
metadata:
  annotations:
    metallb.universe.tf/address-pool: prometheus_ip
  name: prometheus-web
  namespace: monitoring
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - port: 80
      targetPort: 9090
