image:
  repository: minio/minio
  tag: RELEASE.2020-11-25T22-36-25Z-arm
  pullPolicy: IfNotPresent

accessKey: "minio"
secretKey: "melovethanos"

service:
  type: LoadBalancer
  annotations:
    metallb.universe.tf/allow-shared-ip: default

persistence:
  enabled: true
  existingClaim: "minio-pvc"

resources:
  requests:
    memory: 1Gi
