apiVersion: v1
kind: Service
metadata:
  annotations:
    metallb.universe.tf/address-pool: ha_ip
  name: home-assistant-web
  namespace: home-assistant
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: home-assistant
    app.kubernetes.io/instance: home-assistant
  ports:
    - port: 80
      targetPort: 8123
